import { StyleSheet, Text, View } from "react-native";
import React from "react";

type Props = {};

const HistoryContainer = (props: Props) => {
  return (
    <View>
      <Text>historyContainer</Text>
    </View>
  );
};

export default HistoryContainer;

const styles = StyleSheet.create({});
